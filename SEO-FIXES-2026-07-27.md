# SEO fixes applied — July 27, 2026

## Code changes

- Added permanent redirects from the retired city landing URLs to the current city pages.
- Removed the inherited homepage canonical from the root layout.
- Added self-referencing canonicals to the homepage, About page, and FAQ page.
- Shortened the global title template and removed duplicate business-name suffixes from child titles.
- Allowed search engines to crawl Next.js assets by removing `/_next/` from `robots.txt` disallow rules.
- Removed the homepage FAQ schema because the matching FAQ content is not visibly rendered there. The FAQ page still includes matching FAQ schema.
- Replaced the visible keyword-list paragraph on city pages with natural local service copy.
- Removed automatically changing `lastModified` values from the sitemap.
- Normalized the homepage structured-data telephone number.

## After deployment

1. Confirm each old URL redirects to its replacement:
   - `/locations/amity-ar`
   - `/locations/glenwood-ar`
   - `/locations/arkadelphia-ar`
   - `/locations/hot-springs-ar`
   - `/locations/malvern-ar`
2. Open Google Search Console and resubmit `https://www.lynandlilistidyhouse.com/sitemap.xml`.
3. Use URL Inspection on each new city page and request indexing.
4. Inspect each old city URL and confirm Google detects a permanent redirect rather than a 404.
5. Watch the Pages and Performance reports over the following weeks. Do not remove the redirects after the old URLs fall out of the index.

## Validation performed

- Parsed all TypeScript and TSX files successfully with the TypeScript compiler parser.
- Verified all five redirect destinations exist.
- Verified every hard-coded sitemap route has a matching page.
- Verified every page exporting metadata contains a canonical.
- Verified duplicate branded child-title suffixes were removed.

A full `next build` could not be completed in the editing environment because the package registry returned repeated HTTP 503 errors while dependencies were being installed. No dependency or lockfile changes were made.
